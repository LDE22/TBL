<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TBL.Views.ChatsPage"
             Title="Чаты">
    <ContentPage.Content>
        <VerticalStackLayout>
            <!-- Индикатор загрузки -->
            <ActivityIndicator x:Name="LoadingIndicator" IsVisible="False" IsRunning="False" />

            <!-- Список чатов -->
            <ListView x:Name="ChatsListView"
                      ItemSelected="OnChatSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" Padding="10">
                                <Label Text="{Binding LastMessage}" HorizontalOptions="StartAndExpand" />
                                <Label Text="{Binding LastMessageTime, StringFormat='{0:HH:mm}'}" HorizontalOptions="End" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
